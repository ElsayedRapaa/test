/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "static.rain.com",
      "coin-images.coingecko.com",
      "assets.coingecko.com",
    ],
  },
};

export default nextConfig;
